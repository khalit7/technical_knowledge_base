# Operating instructions for agent sessions in this repo

Read `CLAUDE.md`. It is the whole thing, and this file exists only so that
harnesses looking for `AGENTS.md` find it. The skills are shared too:
`.agents/skills` is a symlink to `.claude/skills`, so there is one copy of each
procedure rather than two that drift.
