# 2026-08-25: learning-rate schedulers expanded, WSM paper added

Ad-hoc update requested by Khalid: cover learning-rate schedulers properly (more than one,
with a comparison) and add the WSM paper.

## ml-fundamentals

- **[update] Optimisers and learning-rate schedulers**: the Schedulers section grew from a
  three-row table into a full treatment. Thirteen schedulers now compared on shape, whether
  they need the total step count T up front, and where they are actually used: constant,
  StepLR/MultiStepLR, ExponentialLR, inverse square root (Noam), linear-to-zero, cosine,
  warmup + cosine, SGDR warm restarts, OneCycleLR, ReduceLROnPlateau, WSD/trapezoid,
  schedule-free, and WSM. Added a "how they compare" section (final quality at fixed budget
  is a near-tie; cosine's real cost is lost optionality, not loss; concave cooldowns beat
  linear beat convex; 10-20% cooldown length; final-LR trade-off; the cooldown doubles as a
  data lever) and a short "choosing one" decision list. New best resources: Hägele et al.
  2024 and the PyTorch optim docs.
- **[new] Decay-free schedules subsection** on the same page, covering WSM with its
  equivalence result and headline numbers, cross-linked to the paper page.
- **[new] Annealing vs LR decay** on the same page: the three senses of "annealing"
  (classical LR-lowering, the modern pretraining phase that bundles LR decay with a
  high-quality data switch, and annealing-as-dataset-evaluation), why the two levers are
  separable and usually pulled together in papers, and how to read the terms. The KB already
  described the data anneal in Pretraining and Data mixing but never named the distinction.

## data-curation-and-datasets

- **[update] Data mixing**: cross-link added from the staged-pretraining section to the new
  terminology note.

## llm-training-and-post-training

- **[update] Pretraining**: the LR-schedule practical note now points at the expanded
  scheduler comparison and names the decay-free option.

## Papers

- **[new] WSM: Decay-Free Learning Rate Schedule via Checkpoint Merging for LLM
  Pre-training** (Tian et al., Ant Group Ling team,
  [arXiv:2507.17634](https://arxiv.org/abs/2507.17634), July 2025). Establishes a formal
  equivalence between LR decay and checkpoint merging: merging with weights $c_j$ is the same
  as applying gradient weights $w_i=\sum_{j\ge i}c_j$, and the map inverts uniquely, so any
  monotone decay curve becomes a merge recipe. Constant LR plus merging beat a matched WSD
  decay by ~1.3 points average on a 16.3B/1.4B-active MoE over 400B tokens, and the advantage
  survived SFT. Merge duration dominates checkpoint interval and merge count; EMA merging is
  the weakest, consistent with convex decay curves being the worst shape. Added a row at the
  top of the Papers index.

## agentic-harnesses

- **[new] Personal agents: OpenClaw, Hermes Agent, and how they differ from coding
  harnesses** (`topics/agentic-harnesses/personal-agents.md`). Covers the resident-agent
  category that had no home in the KB: **OpenClaw** (Peter Steinberger, late Jan 2026, Node,
  gateway plus channel adapters, workspace bootstrap files AGENTS.md/SOUL.md/USER.md/
  MEMORY.md, ClawHub skills, pluggable agent runtimes) and **Hermes Agent** (Nous Research,
  Feb 2026, MIT, Python, one AIAgent loop across CLI/gateway/ACP/batch/API, SQLite+FTS5
  session search, autonomous skill creation and curation, first-class agent cron, ShareGPT
  trajectory export). Main section is the harness comparison across invocation, state
  lifetime, world, input trust, verification, undo, permissions, identity, and measurement,
  plus why the relationship is layering (both host or expose coding harnesses over ACP)
  rather than rivalry, and a security section on the category's prompt-injection and
  standing-authority problem.
- **[update] Topic: agentic-harnesses**: new `Personal agents` branch in the taxonomy, a
  landscape paragraph, a `Residency` row in the axes table, and the new file in the file map.

## Structural notes

- No new topic created. Placement rule applied: schedulers belong to the existing
  ml-fundamentals deep dive, not a new page.
- Tracker: unchecked boxes added for `2025-07_wsm` and for this update.
- Repo mirror: nothing done in the Notion session; the next weekly repo sync picks it up
  (including downloading the WSM PDF from the arXiv link).
