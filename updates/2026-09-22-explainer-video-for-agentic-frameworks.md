# 2026-09-22: explainer video for agentic-frameworks

A single addition: the topic overview video for [Topic: agentic-frameworks](../topics/agentic-frameworks/summary.md), plus the one thing the page gained while it was being made.

**[new] A narrated explainer video is attached to **[Topic: agentic-frameworks](../topics/agentic-frameworks/summary.md)**.** Six minutes forty three seconds, 1080p30, one narrator with a second voice asking the viewer's questions four times. It is a topic overview, so it names the whole shelf before explaining any of it: the four layers of the map (orchestration, gateways, observability, memory) with every library on the page named, then a tour along the one axis the page already argues, how much of the agent loop you own. The beats after the map are what the checkpoint buys, what did not survive AutoGen becoming the Microsoft Agent Framework, own the loop against borrow it, gateways and who owns the router now, the convergence (whatever loop you chose you still add a trace per run and a memory store, and OpenTelemetry is settling as the wire format), and the third position that arrived this month, buying the loop.

**[update] That page gained a Video section**, at the top, above the reading-time line. The section says the page is canonical and the video is a derived representation; every figure in it comes from the page and nothing was introduced that the page does not state. No empty Video sections were created anywhere else.

**[update] The Map of the space section now states the organising axis out loud.** Writing the narration made it obvious that the page argues the spectrum in its *Framework vs plain API calls* section and never says it where the map is built, so a reader meets four layers with no reason for the order. One sentence was back-ported: one axis runs through all four layers, how much of the agent loop you own; orchestration is where you pick a position on it, and the other three are what you add once the loop runs. This is the video's explanation returned to the page, not a new claim.

#### Production notes

Nothing was invented for narrative shape. Three figures the page carries were cut rather than shrunk because they did no work in speech: the count of OpenAI Agents SDK primitives, Zep's LongMemEval standing, and the Fugu release dates. No benchmark scores appear, because the page carries none of its own and importing one from a neighbouring page would be the video out-claiming the page.

Two defects in the shared animation furniture were found and fixed while making this one, and both were silent. **`focus`**** did nothing at all**: it decided which map labels were still live by comparing them against the scene's top-level objects, and a parked map's rows are children of the group that was parked, so every label failed the test. The structure check passed, the layout audit was clean, the beat rendered, and the narration said "lit up on the map" over a map that never changed. It was caught by measuring the pixels under the parked headings across beats and finding them identical. **`focus`**** now also takes several labels**, because the convergence beat is genuinely about two layers at once and lighting only the first left the second dimmed while it was being explained.
