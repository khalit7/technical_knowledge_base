# 2026-08-31: weekly update

Scheduled weekly run. Window: 2026-08-25 (the previous digest) to 2026-08-31.

### What matters most this week

1. **Harness scaling stopped being an anecdote.** Two papers, from opposite directions, both beat vendor-native harnesses on identical weights, and both moved ARC-AGI-3 or nine-benchmark agent averages by double digits. Two new Papers entries and a new section on Topic: agentic-harnesses.
2. **GLM-5.3-Flash is out and is the stealth model Ox Alpha.** MIT, 320B-A18B, natively multimodal, 1M context, roughly a tenth of GLM-5.2's price. Patched into Topic: llms and the GLM family page.
3. **Diffusion has found its production job, and it is drafting.** DFlash2 uses block-diffusion drafting inside ordinary speculative decoding, keeping decoding mathematically lossless while claiming up to 3.43x throughput. Patched into Topic: inference-and-serving.
4. **Contamination became checkable.** DeepMind's double-blind evaluation seals weights and benchmark prompts in one encrypted enclave, so neither side sees the other's secret. Patched into Topic: evaluation-and-llm-judges.
5. **Encrypted prompt injection defeats input filtering outright** when the agent has an interpreter, because the agent decrypts its own exploit. Patched into the harness-engineering deep dive.

### New topics and deep dives this week

Beyond the news-driven patches below, five additions that fill standing gaps rather than tracking a release.

- **New Topic: databases**: family taxonomy, an eight-family comparison table (relational, document, key-value, wide-column, graph, vector, time-series, columnar), a how-to-choose section built on access patterns and cross-row invariants, and the cross-cutting concepts (indexing, isolation, CAP, replication and sharding, data lifecycle).
- **New child page, Caching: types, policies, and semantic caching**: eight cache layers from CPU to KV cache, write and read strategies, eviction policies, the three named failure modes (stampede, penetration, avalanche), and a long section on semantic caching as the only cache in the stack with a false-positive rate.
- **New Model formats: GGUF, safetensors, and the rest** under inference-and-serving, comparing seven formats on built-in quantization, load safety, and use case, plus how to read a `Q4_K_M` name. **Quantization and Precision** gains the matching K-quants section (super-blocks, what S/M/L changes, the IQ family, defaults).
- **New AI Engineering Skills Map** under swe-and-system-design: Andrew Ng's five pillars of software fundamentals for AI engineers, mapped onto KB coverage, flagging two real gaps (front-end proper, and conventional application security).
- **Topic: inference-and-serving** gains a five-stage **Best resources: a learning path** section, ordered foundations, transformer internals, GPU and hardware, optimisation techniques, engines.

### Papers

- **[new] RocketEval: Efficient Automated LLM Evaluation via Grading Checklist** (Wei et al., ICLR 2025, [arXiv:2503.05142](https://arxiv.org/abs/2503.05142)). A frontier model compiles each query into 5-10 binary checklist questions once; a 2B judge answers them independently, and the score is read off the normalised Yes/No logit rather than the generated token, which preserves calibration and removes the channel positional bias travels through. Gemma-2-2B reaches 0.965 Spearman with Arena Elo (Mistral-Nemo 0.986, above GPT-4o's own 0.979) at $27.70 per 1,000 tests versus $3,400. Instance-level agreement stays at 57.9% against a 64.7% human-to-human ceiling, and that split is the lesson: ranking a field averages out per-item noise, adjudicating one response does not.
- **[new] Prime Agent: A Self-Improving RLM Harness** (Karten et al., Prime Intellect, [arXiv:2608.23552](https://arxiv.org/abs/2608.23552), 2026-08-24). Open-source harness on a four-level state hierarchy (weights, context, persistent IPython REPL plus live subagents, disk history) under a Recursive Language Model abstraction; a "Continual Harness" versions prompts, memories, skills, and subagent specs across trajectories, and subagents talk to each other over daemon-mediated async queues. ARC-AGI-3 RHAE Best@1 30% to 95.5%; 85.5-hour nanoGPT speedrun with 19 validated records; 7-day Factorio run clearing 24 of 196 technologies. Two findings worth carrying: models are not trained to operate most of what a rich harness offers, and online self-refinement produced genuine specification exploitation (resource-spawning shortcuts in Factorio). Row added at the top of the Papers index.
- **[new] JIT-Agent: Scaling Harness Intelligence via Just-in-Time Harness Evolution** (Zhang et al., NUS et al., [arXiv:2608.25593](https://arxiv.org/abs/2608.25593), 2026-08-26). Factors any harness into `(Memory, Planning, Action, Capability orchestration)`, then trains a 27B model (Qwen3.6-27B; SFT plus bounded repair supervision plus Evo-GDPO against a growing harness archive seeded with 13 hand-written references) to synthesise, repair, and evolve one per task at inference time. +7.7 average for GLM-5.2 and +8.8 for DeepSeek-V4-Flash over nine agent benchmarks, first in 8 of 9 columns, at 14.9-54.1% lower cost than a fixed harness; a JIT-harnessed DeepSeek-V4-Flash beats bare GPT-5.6 on DeepSearchQA (+9.1) and OdysseyBench (+4.3). Row added at the top of the Papers index.
- **[linked, not promoted]** Apodex 1.1 ([arXiv:2608.23283](https://arxiv.org/abs/2608.23283)), the model-side counterpart to the two above; VoiceMem ([arXiv:2608.26005](https://arxiv.org/abs/2608.26005)); WeMM-Embedding ([arXiv:2608.24053](https://arxiv.org/abs/2608.24053)). Listed in this week's news issue only, per the routing rule.

### agentic-harnesses

- **[new] Harness scaling: where the research went** section on Topic: agentic-harnesses. Sets out the three strategies now on the table: give the model more machinery and get out of the way (Prime Agent), constrain it with checked state (StateM), or train a model to build the harness (JIT-Agent). The useful finding is that all three beat the native harness, so the win comes from having a durable state layer at all rather than from a philosophy about control. Records the two stated ceilings (models are not trained to operate rich harnesses; JIT-Agent's four-module protocol is far poorer than what Codex or Claude Code expose, so its parity is parity in a small language), notes Apodex 1.1 as the closest model-side attempt, and carries over Prime Agent's specification-exploitation finding.
- **[update] Harness engineering deep dive**: new dated section on **encrypted prompt injection**, from Adversa AI's cryptographic context injection against Grok (disclosed 2026-06-03, still unpatched). A page ships an AES blob that static filters cannot read; the agent decrypts it in its own Python runtime, manufacturing the malicious plaintext inside the trust boundary after every filter has passed, then exfiltrates chat history, name, location, and subscription tier to an attacker URL. Three transferable lessons written up: a sandboxed interpreter is an injection decoder, egress allowlisting is the control that actually holds, and confirmation gates must fire on what the action does with sensitive state rather than on where the text came from.

### llms

- **[new] GLM-5.3-Flash** ([Z.ai](http://z.ai/), 2026-08-26) added to Topic: llms (dated note plus the taxonomy's GLM node) and to the [Z.ai](http://z.ai/) (Zhipu): GLM family page (lineage entry plus a row in the current-models table). 320B total / 18B active MoE, 1,048,576-token context, image and video input, MIT licence, weights on Hugging Face; claimed to beat GLM-5.2 across [Z.ai](http://z.ai/)'s own evaluations at roughly a tenth the cost ($0.15 / $0.50 per Mtok list). It is the stealth model **Ox Alpha** that the 2026-08-24 news issue flagged as unattributed, which resolves that open item.
- **[update]** GLM-5.3 lineage entry annotated with the reported ~6x coding gain over 5.2 from post-training alone and the claimed CyberGym #1 at 84.5%.

### inference-and-serving

- **[new] Diffusion drafting arrives in speculative decoding** section on Topic: inference-and-serving, covering DFlash2 (z-lab, drafter for Qwen3.8-27B): block-diffusion drafting predicts a whole block per forward pass, keeps top candidates at every position, and runs a light selector over the lattice to trace one path to verify, with two-tap dynamic convolutions countering the usual end-of-block quality decay. Verification is unchanged so decoding stays mathematically lossless (greedy output identical, sampled distribution preserved); up to 3.43x throughput in vLLM or SGLang, MLX supported. Framed against DiffusionGemma: DFlash2 keeps diffusion's parallel-block generation and sidesteps its quality tax by making diffusion a proposal mechanism behind an exact verifier.

### evaluation-and-llm-judges

- **[new] Double-blind evaluation** section on Topic: evaluation-and-llm-judges, covering DeepMind's pilot with the Singapore AI Safety Institute, OpenMined, AVERI, and MLCommons (2026-08-27). Weights and benchmark prompts enter the same hardware-encrypted enclave (Google Cloud Confidential Space), so the lab cannot train on the questions and the evaluator does not receive the model. The point is structural: non-contamination becomes a property of the execution environment rather than a contractual promise, which is the first mechanism that could let one private benchmark be reused across labs and generations without burning it. Caveats recorded: pilot scale (Gemini Flash Lite), the enclave is Google's own infrastructure while Google is also the evaluated party, and it does nothing about benchmark questions leaking into the pretraining crawl from their public source.

### Tech news

- **[new] 2026-08-31: tech news** issue published under Tech news. Top stories: Nvidia agreeing to buy Hugging Face for $12.9B; GLM-5.3-Flash unmasked as Ox Alpha; harness scaling and the two ARC-AGI-3 results; Nvidia's quarter (about 70% guided FY2028 growth, China under 1% of data-center revenue); the 100+ company joint call on AI-enabled cyber attacks. Plus Instinct's $250M Series B, the open-weight-companies-as-acquisition-targets trend, Anthropic's response to infostealer-harvested Claude sessions, the "MCP is dead" argument after the stateless spec, the unpatched Grok injection, and the AI Overviews traffic data.

### Checked and skipped

- **MCP stateless spec (2026-07-28)**: already covered on the MCP page, the protocols topic page, the real-time delivery page, and the auth page. Only the HN argument about it is new, so it went to the news issue and no topic page changed.
- **Claude Code / Gemini CLI / Codex CVEs** (CVE-2026-54316, CVE-2026-12537, fixed in 2.1.163 and 0.39.1): dated 2026-08-07, before the window, and superseded for KB purposes by the more general injection lesson now on the harness-engineering page.
- **OpenAI GPT-Live**: shipped 2026-07-08, outside the window despite resurfacing in this week's roundups.
- **Anthropic / Google / Broadcom 3.5GW TPU deal**: announced 2026-04-06, outside the window.
- **RL, CUDA, PyTorch, JAX, math, hardware**: swept, nothing in the window that clears the bar. PyTorch 2.12 and Triton 3.7.0 remain current with no release in the window.

### Structural notes

- **Scope of this run clarified in the Operating guide.** The weekly procedure now states plainly what the run is for: research the week, add and update topics here, then sync the GitHub mirror, and nothing else. The repo sync is now step 10 of the procedure rather than living only in the scheduled task's prompt. Decisions log records that the run does not read any inbox; requests reach the KB through a session where Khalid asks.
- **One new topic created**, `databases`, against the usual placement rule, because nothing existing fits a general treatment of storage families. Caching went underneath it rather than into swe-and-system-design because the data-layer framing holds the two together; easy to move if that reads wrong.
- No other new topic created. Placement rule applied throughout: harness research into agentic-harnesses, the model release into llms plus the GLM family page, the drafter into inference-and-serving, the eval mechanism into evaluation-and-llm-judges, the injection technique into harness-engineering.
- Tracker: unchecked boxes added for the three new papers, the new databases section, the new deep dives, this digest, the news issue, and the materially updated pages.
- Repo mirror: synced by this same run, including downloading each new paper PDF from arXiv.

### Merged in from the concurrent run (added later on 2026-08-31)

Two sessions of the Monday routine ran against this KB in parallel and each wrote a digest and a news issue. This page is the canonical one; the duplicate has been retitled with a superseded marker and can be deleted. The sections below are the parts of the other run that are not already above. They are complementary rather than contradictory, except where noted.

#### benchmarks

- **[new] Terminal-Bench-Science 0.1** (Stanford plus the Terminal-Bench team): 70 expert-curated tasks across life, physical, Earth, mathematical, and engineering sciences, contributed and reviewed by 376 people across 22 countries, run in the Terminal-Bench sandbox harness. Claude Opus 5 leads at 30.0%, GPT-5.6 Sol 22.4%, Claude Fable 5 21.4%, against roughly 92% for the best agents on Terminal-Bench 2.0. Version 0.2 in development. The natural successor headline once Terminal-Bench 2.x saturates, and the first agent benchmark built around research workflows rather than software engineering. [Announcement](https://www.terminal-bench-science.ai/announcement)
- **[update] ARC-AGI-3 master-table row corrected.** The row read "SOTA ~30% (Aug 2026)", which the week's harness results invalidated: Nvidia's AVO to 100% (Aug 21) and Prime Agent to 95.5% (Aug 24), no change of model. The row now requires the harness to be named alongside any score, and the superseded text moved into a dated toggle. Noted on the page that this is the same integrity failure the methodology page already records for GAIA and WebArena, now hitting the benchmark designed to resist it.

#### protocols

- **[new] Model Hardware Standard (MHS)** added to the agent-protocols section plus a node in the taxonomy. Anthropic research preview, Aug 27: a shared specification for agents to operate physical laboratory and manufacturing equipment, working with any device that has a programmable interface, model-agnostic, reachable by any harness over standard protocols including MCP. It is a device-abstraction layer above MCP, not a competing transport. Early results: real-time error handling in a Genentech drug-discovery run, a Janelia imaging experiment compressed from weeks to a day, QuEra laser stabilisation from 58% to 99.3%. Anthropic intends to open-source it. The KB note flags the trust boundary: MCP's injection and confused-deputy failures are recoverable, an agent driving a liquid handler is issuing irreversible physical actions, which is the standing-authority problem the personal-agents page raises for resident agents.

#### hardware

This supersedes the "hardware: swept, nothing in the window that clears the bar" line in the Checked and skipped section above; two items did clear it.

- **[new] AMD MI400 rack detail from Hot Chips 2026**: 72 GPUs per Helios rack, 2.9 exaflops, 31 TB HBM4, 1.7 PB/s aggregate HBM4 bandwidth (roughly 430 GB at about 24 TB/s per GPU). Ahead of Vera Rubin NVL144 on memory per GPU, and confirmation that the rack rather than the card is the unit of comparison on both sides now. ROCm remains the gating factor and remains stronger for inference than training.
- **[new] Nvidia Q2 FY27 (Aug 26)** recorded as the demand denominator: $96.2B revenue (up 106% year on year), $89.0B of it datacenter (up 117%, up 18% sequentially) on the Blackwell Ultra ramp, $108B guided for the current quarter, non-hyperscaler datacenter revenue growing fastest at 138%.

#### llms

- **[new] Qwen3.8-Flash-Next** (Alibaba, Aug 26): open-weight preview of the **Qwen4 architecture**, released early so the ecosystem can prepare. 125B total / 6B active MoE (about 21x sparsity, the most aggressive in the open frontier), causal LM plus a vision encoder, 262,144-token native context extensible to 1M. The architectural delta is **Qwen Sparse Attention (QSA)** replacing Gated Attention alongside Gated DeltaNet, selecting at micro-block rather than individual-token granularity because block selection is hardware-friendly in a way token-level selection is not. That puts the whole open frontier on trainable sparse or linear attention (DeepSeek DSA/CSA, Kimi KDA, MiniMax MSA, Qwen QSA), and moves the competitive axis from total parameters to active-parameter efficiency. [Qwen blog](https://qwen.ai/blog?id=qwen3.8-flash-next), [Hugging Face](https://huggingface.co/Qwen/Qwen3.8-Flash-Next)

#### Papers, link only

- **Apodex 1.1: Scaling Agentic Intelligence for Complex Work** ([arXiv:2608.23283](https://arxiv.org/abs/2608.23283), Aug 24): 75-author report on a 35B locally deployable agent model trained against a shared execution harness and "AgentOS". Linked from the harness-scaling section rather than given a full paper page, per the routing rule. Promote on request.

#### Process note

The 07:00 UTC scheduled run was interrupted after patching Topic: llms and Topic: agentic-harnesses and creating the two paper pages. Two sessions then picked the work up independently, which is how the duplicate digest and news issue arose. Worth a guard in the routine if it recurs: check Updates and Tech news for a page already dated today before creating one.

### Cleanup pass (added 2026-08-31, after the above)

A third session had also been running the blog-processing job against this KB in parallel, so the duplication was wider than the two digests noted above. Consolidated as follows. Nothing was hard-deleted.

**Duplicate topic page.** Two `Topic: databases` pages existed. The canonical one is now [Topic: databases](../topics/databases/summary.md), which is the fuller of the two: twelve families rather than eight, a six-column comparison table, a cross-cutting-axes section, an ML and LLM section, a quick chooser, and a `Storage engines, indexes, and the physics of a query` deep dive. Four things existed only on the other version and were folded in: the Andrew Ng data-management framing, the five ordered questions now under "How to actually choose", the data-lifecycle point, and The Log. The other page is retitled `Topic: databases [SUPERSEDED 2026-08-31, see Topic: databases]` and carries a callout saying so.

**Duplicate caching page.** Two also existed, one under databases and one under swe-and-system-design. The canonical one is [Caching: types, policies, and semantic caching](../topics/databases/caching.md), kept where the Tracker and this digest already pointed, under Topic: databases. Merged in from the other: the provider prompt-cache economics table (Anthropic, OpenAI, Gemini read and write multipliers, minimum prefix lengths, TTLs), the KV-cache per-token byte formula, three more cache layers (OS page cache, reverse proxy, materialised views), Redis's real maxmemory-policy menu, the hot-key and stale-read-under-replication failure modes, cost-weighted hit ratio, the named semantic-cache implementations, and a closing "which cache do I reach for" list. The duplicate is retitled with a superseded marker. The stale row was removed from Topic: swe-and-system-design's deep-dives table and a `topics/databases` line added to its Related topics.

**Model formats and RocketEval** were each written twice but caught before publishing a second page; both were corrected in place rather than duplicated. The RocketEval Results section had listed AlpacaEval and Arena-Hard as experiment benchmarks, which the paper does not use: corrected to MT-Bench and WildBench.

**Root page checked, no change needed.** An earlier version of the root page said the weekly run sweeps the blog entries database before doing anything else, which contradicted the binding decision **"2026-08-31: The weekly run's scope is fixed"** added later to the Operating guide, and contradicted the Blog page's own statement. By the time this cleanup pass reached it, a later run had already removed that bullet. Root page, Operating guide and Blog page now agree: **blog entries are picked up when Khalid asks for the blog to be processed, not by the weekly run.**

**Tracker**: `storage-engines-and-indexes.md` added unchecked under `databases`.

**For the next run**: the guard suggested in the process note above is worth taking seriously. Three sessions wrote to this tree within one hour. Check Updates and Tech news for a page already dated today, and check the root page's child list for a `Topic:` name that already exists, before creating either.
