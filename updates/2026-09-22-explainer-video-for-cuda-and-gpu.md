# 2026-09-22: explainer video for cuda-and-gpu-programming

A single addition: the topic overview video for [Topic: cuda-and-gpu-programming](../topics/cuda-and-gpu-programming/summary.md), plus the two things the page gained while it was being made.

**[new] A narrated explainer video is attached to **[Topic: cuda-and-gpu-programming](../topics/cuda-and-gpu-programming/summary.md)**.** Six minutes twenty five seconds, 1080p30, one narrator with a second voice asking the viewer's questions three times. It is a topic overview, so it names the whole board before explaining any of it, and the board is this topic's real axis: not a field of competitors but three layers of one stack. The machine (threads, warps, blocks, grids; registers, shared memory, L2, HBM; tensor cores), where you are allowed to write (CUDA C++ through nvcc, CUTLASS and CuTe, Triton and Gluon, and this month cuda-oxide and cutile-rs), and what you call instead of writing anything (cuBLAS, cuBLASLt, cuDNN, NCCL, Thrust, CUB, Nsight Systems and Nsight Compute). The map is parked as the home frame and every later beat lights the part of it being discussed.

The organising question is the page's own sentence, nearly all kernel optimisation is memory optimisation, and the tour answers it. The hierarchy, drawn as a stack, with the single idea every optimisation turns out to be: get the data up the list once and do all the work you can while it is up there, which is what tiling, coalescing, fusion and FlashAttention all are. Then the levels you may write at, as a grid of what you think in and what it costs you: threads and ten times the code, layouts and tiles and template work, blocks and zero to twenty percent off the CUDA version, and Gluon handing the layouts back. Then the two CUDA Rust tracks, which reproduce that grid in a second language, cuda-oxide making aliasing between concurrent threads a compile error and cutile-rs getting exclusive access out of ordinary ownership and already shipping in Hugging Face's Grout engine and mistral.rs. Then the layer most people should live in, and the profiler order: Nsight Systems for the timeline before Nsight Compute for one kernel. It closes on the map as a ladder you climb down rather than up, and on the failure that costs a week, hand optimising arithmetic on a machine that was waiting for memory.

**[update] That page gained a Video section**, at the top, above the reading-time line. The section says the page is canonical and the video is a derived representation; every figure in it comes from this page and nothing was introduced that the page does not state. No empty Video sections were created anywhere else.

**[update] Map of the space now says two things out loud that it only implied.** Writing the narration made both gaps obvious. The page asserted that nearly all kernel optimisation is memory optimisation and never said why, so the reason is now on the page: a GPU can perform far more arithmetic per second than it can fetch operands for, which makes the binding constraint bytes moved rather than operations issued. And the page held every rung of the ladder without ever stating the order to try them in, so that is there too: call the library first, write Triton when that is not enough, reach for Gluon when the compiler's heuristics cost you, and go down to CUDA C++ only when a profiler says the last 0-20% is worth ten times the code. Both are the video's explanation returned to the page, assembled from what the page already states rather than new claims.

#### Production notes

The inventory axis came from the cut of this episode that was cleared when the method changed, and it is the one thing worth keeping from it: this topic's map is layers of a stack, not a list of things that do the same job differently. Following the page's own taxonomy diagram instead produces a tour of a tree, with profiling sitting between libraries and DSLs for reasons that are about the diagram rather than about the subject.

A 2,225-word page does not fit in six minutes and a good deal was cut rather than compressed; the script's docstring records what. The omission that hurts most is the tensor-core instruction vocabulary, wgmma, TMA, tcgen05 and TMEM, and with it the RTX 5090 catch: sm_120 exposes TMA and clusters and none of the rest, so Hopper tutorials do not literally run on it. That is parent-page material and it costs somebody an afternoon. It went because the page itself says those names are defined a page down, under CUTLASS and tensor cores, and because a vocabulary list is what a tour of three layers can most afford to lose. Tensor cores stay named on the map, and the organising-question beat points at them as the reason the arithmetic outruns the memory. Also cut: the Cohere megakernel result, which is the one number on the page that pushes the other way and whose full treatment the page already puts on inference-and-serving; the whole AMD section, ROCm 10.0 and the ROCm CLI and Hyperloom and AMD Skills, which is a second episode with an argument of its own about how operational knowledge reaches an engineer; the seven-step learning path, which is a page to work through rather than a thing to watch; Dream-RSI and Phi-Bench; and CUDA on RISC-V hosts.

Two defects were found by the checks rather than by watching. **A chart implied a verdict nobody made.** The two Rust tracks were drawn as cost against verified, which says the tile track is the right answer and the thread track is a mistake. NVIDIA shipped both, published no performance numbers, and the page calls them two ambitions. cuda-oxide is now the machinery colour, which is what a compiler backend is, so the colour carries the distinction without the judgement. **Reveals lagged the narration by up to eighteen seconds.** A panel's items are spread evenly across whatever is left of the spoken line, so a beat that names four things in its first half draws the fourth in its second half. `check_references` pulled the frame for "Above it, CUTLASS and CuTe" and it held the CUDA C++ row and nothing else. Every `reserve` in the episode was then set from the rendered beat lengths rather than guessed, so each row lands as it is named and what remains held back is the beat's closing argument spoken over a finished panel.

One take needed rerolling. The map beat is a list of thirteen names and came back with a stumble in the profilers, which the error rate did not catch because character-level agreement stayed at 0.05; reading the transcript as text did. Four seeds were tried, one of which read cleanly but carried a burst of non-speech at 0.68, which the error rate also passed. The published take is the third attempt at character error 0.026 with no-speech 0.04.

On length, which is a deviation worth stating. The skill says plan at about 150 words a minute and aim to land under six. The script is 947 words and lands at 6:25 and 4.14 MiB, so it delivers at 1080p, one rung down the rate-factor ladder. The first draft ran to 1,253 words and about eight and a half minutes, which is near where the encode ladder runs out altogether; the beat that bought most of the cut was the tensor-core vocabulary.
