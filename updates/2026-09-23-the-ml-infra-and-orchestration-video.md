# 2026-09-23: the ml-infra-and-orchestration video re-cut

A re-cut rather than a new episode. The topic overview for [Topic: ml-infra-and-orchestration](../topics/ml-infra-and-orchestration/summary.md) is replaced, for timing only. The axis, the four-question map, the organising question, the nine beats, the take and every spoken word are exactly as published, and all nine takes are the audio that shipped, byte for byte. Only the panels changed.

**[update] The video on **[Topic: ml-infra-and-orchestration](../topics/ml-infra-and-orchestration/summary.md)** is re-cut, and the old one is taken down.** The published cut named eleven reveals before the animation drew them, the worst 8.2 seconds, on Kueue in the schedulers beat, and five further reveals could not be timed at all, which reads exactly like a pass and is not one. Most of those leads were invisible when it shipped, because check_leads did not yet model the focus delay. Still six minutes forty-seven seconds, still 1080p, now 4.66 MiB against 4.64 before. The new cut has no lead over 2.7 seconds, measured from the voice's own word timestamps, and no still frame over 5.42.

#### Production notes

**The focus delay is most of what the first cut could not see.** A beat carrying focus spends a quarter of a second per map handle lighting the parked map before it draws anything, and this map registers twenty handles, sixteen items and four column headings, so schedulers, pipelines, machines, watch and close each lose five seconds off the front. On schedulers and close that delay sat in front of a points heading, which is a reveal of its own, so every row behind it was pushed one place later.

**Two headings went, three were reworded, and five rows were added, every one for a sentence the beat already speaks.** Schedulers and close lost their headings outright. Schedulers split Volcano from the training operators, so Kueue lands a place earlier, and split its closing sentence into a Slurm row and a Kubernetes row. Machines split the glue that decides your bill from the S3 layout with Lambda and EventBridge, which is the beat's last sentence and had nothing to land on. Watch split Weights and Biases from MLflow. The machines and watch headings, and two Ray rows, were reworded to the words the narration actually says, which is what made the five untimed reveals timeable; none of the five turned out to be leading.

**Reserves went to the midpoint, except where the word timestamps disagreed with the estimate.** check_leads --reserves suggests the value midway between the lead floor and the still-frame cap, and map, question, ray, machines, watch and close took it: 7.7, 2.8, 2.8, 3.7, 2.9 and 4.4. Schedulers and pipelines did not. On the suggested values, check_leads --words put their last reveals 3.3 and 3.1 seconds ahead of the words, where the even-pace estimate had them inside tolerance, so they sit at 5.0 and 4.5. A matcher change also landed mid-repair, letting a label skip a word the narration never says, and it timed the old Slurm-and-Kubernetes row from "Slurm still wins", three seconds earlier than before; that is why the row was split.

**What the checks say now.** check_leads --words: every timed reveal is named at or after the moment it is drawn, worst 2.7 seconds, and nothing is untimed. check_timing: nine beats, none overlapping, no silence over four seconds, 143 words a minute overall, no beat above 155, every still frame between 2.5 and 5.42 seconds. check_structure and the layout audit are clean. verify.py passes all nine takes at character errors between 0.000 and 0.047. check_references finds six phrases, all ordinary non-deictic English. No panel changed kind and the map the narration counts rows on is untouched, so no spatial word in the narration was falsified.

**The video was not watched.** Nothing here can play a file. The proxies were a frame of each re-panelled beat in its finished state, the six reference frames, the unchanged transcripts, and the per-beat pace table. Whether the close still reads as a take without its heading is not covered by any of that.
