# 2026-09-23: explainer video for programming-languages

A single addition: the topic overview video for [Topic: programming-languages](../topics/programming-languages/summary.md), plus the one thing the page gained while it was being made.

**[new] A narrated explainer video is attached to **[Topic: programming-languages](../topics/programming-languages/summary.md)**.** Six minutes fifty-four seconds, 1080p30, one narrator with a second voice asking the viewer's question three times. It is a topic overview, so it names the whole board before explaining any of it, and the board is the page's own five tracks: Python (the data model, async IO, hot code specialising itself), C++ (object lifetime, RAII, undefined behaviour), Rust (ownership, unsafe and Miri, GPU kernels), and JavaScript with TypeScript in one column, because the page treats TypeScript as a type language over JavaScript rather than as a sixth track.

Read as a comparison this page turns into “which language should I learn”, which it never asks and cannot answer, so the organising question is what each one is FOR: the five are not alternatives, they sit at different depths of one stack and you meet each for a different reason. The shared five-stage ladder, with a gate question at each stage, is what makes that answerable, and it is the beat that carries the question. Then one beat per depth. Python as the working language, where the ladder aims at what you can miss for years rather than at the syntax. C++ and Rust as a single table answering one question, who owns the lifetime of an object, in opposite ways: the programmer against the compiler, a program with no defined meaning against a program that will not build, and Miri against nothing. JavaScript and TypeScript as four rules rather than four surprises. And the newest material on the page, which is who gets to write a GPU kernel: cuda-oxide at thread level, cutile-rs at tile level, and Mojo. The take is that the map is a router rather than a curriculum, and that wherever you start, you start at the stage three gate.

**[update] That page gained a Video section**, at the top, above the reading-time line. The section says the page is canonical and the video is a derived representation; every figure in it comes from this page and nothing was introduced that the page does not state. Nothing was imported from the seven deep dives underneath it. No empty Video sections were created anywhere else.

**[update] The page now expands MCP on first use**. The JavaScript track says agent harnesses, MCP servers and dev tooling live there, and the page never says what MCP stands for, anywhere on it. A reader can carry an unexpanded acronym; a listener cannot, and this method requires every acronym expanded on first use in speech. Saying “Model Context Protocol servers” out loud made the page the weaker of the two, so the expansion went in before the script was rendered.

#### Production notes

**The inventory axis came from the cut of this episode cleared when the method changed, which found it independently, and it is the page's own. **The page's opening sentence is that all five languages are written zero to expert, and its five-stage ladder exists so that progress means the same thing across all of them, which is a mental model rather than a comparison. The alternative axis is a tour of the seven deep dives in the order the table lists them, which is a table of contents read aloud. Five things from the cleared cut are not kept and the script's docstring records them: ten beats with no reserve on any of them, a two-reveal claim card for the question, a two-reveal stat for Mojo on a seventy-word line, a compare panel carrying a hundred and fifty word systems beat, and a map that drew C++ in the cost colour against Rust in the verified colour. That last one is a verdict on screen that this page refuses: it keeps C++ as the substrate under every inference engine and every kernel, so C++ and JS/TS share the machinery tone here and no column is cost.

**A four column map is not the roomy case, and this episode was written against the old version of that rule. **The method was corrected mid-run by another episode: panel_columns derives the pill from the column count, max(2.5, min(4.2, (free - 0.7 x (n-1)) / n)), and 2.5 is a floor, so a four column map beat is exactly as tight as one sitting beside a parked map, about fifteen characters rather than the twenty-two that has shipped on three column maps. Three items were shortened on that news. The fourth, “undefined behaviour” at nineteen characters, was kept deliberately and checked on a frame: it is the C++ column's load-bearing item, every shortening of it is either cryptic or wrong, and “UB” would also orphan, since the narration says the words rather than the letters. Its column is visibly a little wider than the other three and the board does not scale down.

**The map beat read at prose pace, which contradicts the rule that an inventory beat runs thirty to forty words a minute slower, and the newer note that four column maps are slowest of all. **It came back at 139 and 145 on two seeds, against a budget of 100 taken from the method. The reason is the syntax rather than the column count: the measurements behind those rules are map beats that are lists of bare product names separated by full stops, and this one is four ordinary sentences with the names inside them. Budget a map beat by how it is written, not by how many columns it has. The episode-wide figure was wrong in the other direction: the method's 0.40 seconds a word is a median and this narration delivered at 0.42, which is thirty seconds across an episode and is the difference between planning for 6:38 and shipping 6:54.

**Three defects were caught only by reading the transcripts as text, and every one of them passed the whole gate. **The opening beat grew the invented word “Disadvance” out of nothing, straight after “Working knowledge is assumed nowhere”, at a character error of 0.055: one word is not a burst, so the invented-word detector cannot see it, and a seed reroll on the same text cleared it. “asyncio” came back as “a CO” on the map beat and “a CCO” on the Python beat, on two different seeds, which is the lowercase-compound failure this method already names; written “async I O” the transcriber returns the real name, and both panels were respelled “async IO” so the orphan check's squashed contiguous match still holds. And “the ladder” came back as “the latter” in the Python beat, then, once that beat was rewritten, in the map beat instead. Every word there is ordinary English and no gate can fire; the plural “five ladders” transcribes correctly in three other beats, so it is the singular after a wh-word that invites it. Written “whose ladder is aimed at” it was right first time. That is the unadjudicable case the method describes for an elided “that”, and the same answer applies: do not decide whether the voice or the transcriber was wrong, write it so neither can be.

**One lead is accepted rather than fixed and one defect was judged and left, both of which the method now allows and both worth stating. **The frontier beat names cuda-oxide 3.4 seconds before its table row is drawn. Neither lever is available: a table's reveal count is fixed by the data, closing the lead wants a reserve of 6.7 against a still-frame ceiling of 5.6, and moving the words means opening a sentence with a fragile name and re-rolling a beat that is clean at a character error of 0.005. Two seconds is noise and six is a rewrite, so this is an accept. Separately, that beat ends on “at one point zero” and the transcript reads “at one point. And zero.” One inserted function word at 0.005 is most likely the transcriber segmenting a number, and a reroll forced on a clean take has no fallback, so it ships. It is the exposed position the method warns about and the next revision should end that beat on a word rather than on a version number.

**On pace, which cost one reroll and five seconds of episode. **The take came back at 173 words a minute, well past the 165 that check_timing fails on. Splitting its five turns into nine and turning its commas into full stops brought it to 154 without cutting a word, which is inside the writing target rather than merely inside the gate. The opening beat had the same problem in the other direction before the voice existed: at 160 it got the punctuation fix and came back at 134. Both are the method's cheapest lever and both worked first time.

**The video was not watched. **Nothing here can play a file, so the proxies were used and are worth naming: a frame pulled per beat and read by eye, including the completed board taken between beat minus reserve and two seconds later, which is the only window in which a parked map stands still; every transcript read as text against its line, which is what caught all three defects above; check_timing, which reports no overlap, no silence over four seconds and no beat above 165; check_leads, which is down to the one accepted lead; check_references, whose four hits were read on their frames and are three ordinary English words plus one real pointer, “the ladder on screen”, which lands with two of the five rungs drawn and is the partial reveal the method says is fine; and the layout audit, which is clean. Pace, whether the argument lands and whether the take is worth hearing are not covered by any of that.
